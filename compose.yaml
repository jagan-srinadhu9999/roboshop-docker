services:
  mongodb:
    image: jaganf8/mongodb:1.0.0
    container_name: mongodb
    ports:
      - "27017:27017" 
    volumes:
      - mongo_data:/data/db
    networks:
      - roboshop
volumes:
  mongo_data:
  redis_data:
  mysql_data:
  rabbitmq_data: